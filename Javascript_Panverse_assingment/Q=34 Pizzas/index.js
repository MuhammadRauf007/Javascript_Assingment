let Pizza_arr = ["Pepperoni","Fresh Veggie Pizza","Chicken Sausage Pizza","Chicken Dominator Pizza"];

for (let i = 0; i < Pizza_arr.length; i++) {
    const Names = Pizza_arr[i];
    console.log(`${Names}`);
}

for (let j = 0; j < Pizza_arr.length; j++) {
    const Sentence = Pizza_arr[j];
    console.log(`I like ${Sentence} pizza.`);
}
